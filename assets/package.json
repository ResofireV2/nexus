{
  "name": "nexus-assets",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js --deploy",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "marked": "^12.0.0",
    "dompurify": "^3.1.5"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
